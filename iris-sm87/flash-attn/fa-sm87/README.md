# Experimental SM87 FlashAttention

Reserved for the forward-only SM87 implementation after the LLaVA baseline, reference tests, and attention contract are established.

Use the adjacent `fa-v1` crate as the Ampere-compatible baseline. Keep `fa-v3` as Hopper-only reference material; do not port its TMA/WGMMA implementation directly.
